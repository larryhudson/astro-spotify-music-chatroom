export async function getUserIdFromRequest(request) {
  // TODO: implement this.
  // get the spotify auth token from the cookie, and then do the spotify request to get the user id.
  // maybe we should save that somewhere? cache it?
  return null;
}
